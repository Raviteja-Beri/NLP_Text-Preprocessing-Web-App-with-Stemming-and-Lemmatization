# Text Preprocessing Web App with Stemming and Lemmatization

This repository contains a **Natural Language Processing (NLP)** application built with **Python**, **Flask**, and **NLTK**.  
It provides multiple text processing functionalities such as **tokenization**, **stemming**, and **lemmatization**, accessible via both a **web interface** and a **command-line tool**.

---

## 🚀 Features

- **Sentence Tokenization** → Splits text into sentences.
- **Word Tokenization** → Splits text into words.
- **WordPunct Tokenization** → Splits text into words & punctuation separately.
- **Stemming** → Reduces words to their root forms using **Porter Stemmer**.
- **Lemmatization** → Converts words to base form using **WordNet Lemmatizer**.
- **Stopword Removal** → Removes common English stopwords for cleaner results.
- **Flask Web Interface** → Interactive UI for entering text and choosing processing method.
- **CLI Script (`tokening.py`)** → Direct terminal-based execution for NLP tasks.

---

## 🛠️ Tech Stack

- **Python 3.8+**
- **Flask** → Web framework for interactive interface
- **NLTK** → Tokenization, stemming, lemmatization, and stopword filtering
- **HTML (Jinja2 templates)** → For rendering results on the frontend

---

## ⚙️ Installation & Setup

1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/nlp-text-processing.git
   cd nlp-text-processing
   ```
2. **Install dependencies**
```bash
pip install -r requirements.txt
```
3. **Run the Flask App**
```bash
python app.py
```
---

## 📊 Example Usage
- Web Interface
- Enter any text in the input box.
- Select processing method (Sentence, Word, WordPunct, Stem, Lemmatize).
- View results instantly on the UI.

---
## Thank You
---


